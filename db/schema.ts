import{sqliteTable,text,integer}from"drizzle-orm/sqlite-core";
export const members=sqliteTable("members",{id:text("id").primaryKey(),membershipNumber:text("membership_number").notNull().unique(),firstName:text("first_name").notNull(),lastName:text("last_name").notNull(),phone:text("phone").notNull().unique(),email:text("email"),passwordHash:text("password_hash").notNull(),category:text("category").notNull(),state:text("state").notNull(),lga:text("lga").notNull(),ward:text("ward").notNull(),pvcStatus:text("pvc_status"),politicalAffiliation:text("political_affiliation"),referralCode:text("referral_code").notNull().unique(),status:text("status").notNull().default("new"),createdAt:text("created_at").notNull()});
export const settings=sqliteTable("settings",{key:text("key").primaryKey(),value:text("value").notNull(),updatedAt:text("updated_at").notNull()});
export const organizationUnits=sqliteTable("organization_units",{id:text("id").primaryKey(),name:text("name").notNull(),type:text("type").notNull(),parentId:text("parent_id"),active:integer("active",{mode:"boolean"}).notNull().default(true),sortOrder:integer("sort_order").notNull().default(0)});
export const positions=sqliteTable("positions",{id:text("id").primaryKey(),title:text("title").notNull(),unitId:text("unit_id"),parentPositionId:text("parent_position_id"),allowsDeputy:integer("allows_deputy",{mode:"boolean"}).notNull().default(true),allowsAssistant:integer("allows_assistant",{mode:"boolean"}).notNull().default(true),active:integer("active",{mode:"boolean"}).notNull().default(true)});
export const whatsappGroups=sqliteTable("whatsapp_groups",{id:text("id").primaryKey(),name:text("name").notNull(),inviteUrl:text("invite_url").notNull(),unitId:text("unit_id"),active:integer("active",{mode:"boolean"}).notNull().default(true)});
